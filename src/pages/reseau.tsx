import type { ReactNode } from "react";
import Layout from "@theme/Layout";

export default function Reseau(): ReactNode {
    return (
        <Layout>
            <main className="container margin-vert--lg">
                <h1>Notre réseau</h1>
            </main>
        </Layout>
    );
}